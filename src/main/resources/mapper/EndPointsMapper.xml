<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.EndPointsMapper">

    <select id="getEndPoints" resultType="hashmap" parameterType="hashmap">
        select
        id,
        service_id,
        name,
        path,
        method,
        description,
        request_schema,
        response_schema,  <!-- 修正拼写错误：reponse_schema -> response_schema -->
        default_params,   <!-- 推测default_schema应为default_params -->
        is_active,
        created_at,
        updated_at
        from api_endpoints
        <where>
            <if test="serviceId != null">
                and service_id = #{serviceId}
            </if>
            <if test="name != null">
                and name = #{name}
            </if>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="description != null">
                and description like concat('%', #{description}, '%')
            </if>
        </where>
    </select>
</mapper>
